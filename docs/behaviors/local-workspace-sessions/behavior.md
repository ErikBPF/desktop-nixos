lets explore how to have default folders to open terminal based apps and session based on what workspace we are. 2-4 show open dataplatform and 5-12 should open homelab

lets have 8 default sessions to always start on windows 2 and 7 on 3 and 8 we always open a tuicr and a neovim session

6 with our defalt coding agent(now is codex, but this should be a var). 2 in shell

lets run from this computer

persistent sessions. Login + shortcut to recover

lets do a /pl /ip and /rv. Then implement

apply

the 8 windows show be independet tmux sessions

check opening layout to make it create windows in a grid pattern

we shold use the default layout an use some sort of hyprctl command to position windows
